# object_decetion 虚拟环境依赖
# 说明：
#   - 不包含系统 ROS2 库（rclpy、cv_bridge 等通过 --system-site-packages 继承）
#   - 不包含 opencv-python（使用系统 cv2，避免与 cv_bridge 的 numpy 冲突）
#   - numpy 必须 <2.0，否则与系统 cv_bridge 不兼容
#   - torch/torchvision 为 CUDA 12.8 版本，CPU 环境请见下方注释

# ── 核心推理库 ────────────────────────────────────────────────────────
torch==2.10.0
torchvision==0.25.0
numpy==1.26.4

# ── YOLOv8 运行依赖 ───────────────────────────────────────────────────
lapx==0.9.4
matplotlib==3.6.3
pandas==2.3.3
Pillow==10.2.0
PyYAML==6.0.1
requests==2.31.0
scipy==1.11.4
tqdm==4.67.3
seaborn==0.13.2
psutil==5.9.8
py-cpuinfo==9.0.0
sympy==1.14.0
filelock==3.25.2
fsspec==2026.2.0
networkx==3.6.1

# ── CUDA 运行时（torch 依赖，GPU 环境自动安装）─────────────────────────
# 如果是纯 CPU 环境，以下可以不装（torch CPU 版不需要）
nvidia-cublas-cu12==12.8.4.1
nvidia-cuda-cupti-cu12==12.8.90
nvidia-cuda-nvrtc-cu12==12.8.93
nvidia-cuda-runtime-cu12==12.8.90
nvidia-cudnn-cu12==9.10.2.21
nvidia-cufft-cu12==11.3.3.83
nvidia-cufile-cu12==1.13.1.3
nvidia-curand-cu12==10.3.9.90
nvidia-cusolver-cu12==11.7.3.90
nvidia-cusparse-cu12==12.5.8.93
nvidia-cusparselt-cu12==0.7.1
nvidia-nccl-cu12==2.27.5
nvidia-nvjitlink-cu12==12.8.93
nvidia-nvshmem-cu12==3.4.5
nvidia-nvtx-cu12==12.8.90
cuda-bindings==12.9.4
cuda-pathfinder==1.4.3
triton==3.6.0
